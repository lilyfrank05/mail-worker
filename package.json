{
  "name": "mail-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: API-key-authenticated HTTP endpoint that sends emails via the Email Service binding",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260813.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.122.0"
  }
}
