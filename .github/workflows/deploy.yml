name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: deploy
          postCommands: |
            echo "$EMAIL_WORKER_API_KEY" | npx wrangler secret put EMAIL_WORKER_API_KEY
            echo "$FROM_ADDRESS" | npx wrangler secret put FROM_ADDRESS
        env:
          EMAIL_WORKER_API_KEY: ${{ secrets.EMAIL_WORKER_API_KEY }}
          FROM_ADDRESS: ${{ secrets.FROM_ADDRESS }}
